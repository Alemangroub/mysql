
# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0

# Environment variables and secrets.
env:
  # Grant access to the consolidated Firebase Admin SDK config secret.
  # This secret contains all necessary credentials in a single JSON object.
  - variable: FIREBASE_ADMIN_SDK_CONFIG
    secret: FIREBASE_ADMIN_SDK_CONFIG
    availability:
      - BUILD
      - RUNTIME
